import 'package:flutter/material.dart';

class DiscussionLoader extends StatelessWidget {
  const DiscussionLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
        child: Center(
      child: Text('Liste des messages...'),
    ));
  }
}
